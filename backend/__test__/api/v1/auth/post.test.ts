describe("Auth POST API routes.", () => {
  test("Given a new user, when registering a user, create new user", async () => {
    expect(1).toEqual(2);
  });

  test("Given an existing user, when registering a user, return error", async () => {
    expect(1).toEqual(2);
  });

  test("Given an existing user, when logging in with valid credentials, sign user in", async () => {
    expect(1).toEqual(2);
  });

  test("Given an existing user, when logging in with invalid credentials, return error", async () => {
    expect(1).toEqual(2);
  });

  test("Given an existing user, when resetting password with valid credentials, reset password", async () => {
    expect(1).toEqual(2);
  });

  test("Given an invalid user, when resetting password with invalid credentials, return error", async () => {
    expect(1).toEqual(2);
  });
});
