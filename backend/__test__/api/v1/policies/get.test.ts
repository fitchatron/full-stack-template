describe("Policies GET API routes.", () => {
  describe("Get all polices.", () => {
    test("Given a user with the read policies policy, ", async () => {
      expect(1).toEqual(2);
    });
  });
});
